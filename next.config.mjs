/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "uxzphkhhygtnotcapxvv.supabase.co",
      },
    ],
  },
};

export default nextConfig;
